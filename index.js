const sort = require('./src/sort');
const {compare, sortArrayOfObjects, sortArraysInObject} = require('./src/comparison');

module.exports = {
    sort,
    sortArrayOfObjects,
    sortArraysInObject,
    compare
};